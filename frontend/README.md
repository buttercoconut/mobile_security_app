# Mobile Security App Frontend

This is a minimal Vue 3 SPA that demonstrates how to interact with the backend security APIs.

## Running locally

```bash
cd /home/yutw/project/md_templates/results/mobile_security_app/frontend
npm install
npm run dev
```

The app will be available at `http://localhost:3000`.
